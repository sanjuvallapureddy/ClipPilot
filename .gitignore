# NOTE: .env is intentionally committed so teammates get all config/keys on pull.
# This is ONLY acceptable because this repo is PRIVATE. If it ever becomes public,
# remove .env from git history and rotate every key in it immediately.
client_secret*.json
# Per-machine local overrides (never committed):
.env.local
.env.*.local
__pycache__/
*.pyc
.venv/
.venv-harness/
venv/
node_modules/
.next/
dist/
build/
*.mp4
*.log
.DS_Store
media/               # downloaded source videos (Lane C ingest)
engine/openshorts/   # vendored OpenShorts (add as submodule / git clone)
openshorts/          # vendored OpenShorts (cloned at repo root)
